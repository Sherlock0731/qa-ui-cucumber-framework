<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>qa.autotest</groupId>
    <artifactId>ui-test-cucumber-framework</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>UI Test Automation Framework</name>
    <description>Multi-threaded UI testing framework with JUnit5, Selenide, Allure</description>

    <properties>
        <!-- Java Version -->
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependencies Versions -->
        <junit.version>5.10.1</junit.version>
        <cucumber.version>7.15.0</cucumber.version>
        <selenide.version>7.0.4</selenide.version>
        <selenium.version>4.25.0</selenium.version>
        <webdrivermanager.version>5.6.3</webdrivermanager.version>
        <assertj.version>3.24.2</assertj.version>
        <allure.version>2.25.0</allure.version>
        <lombok.version>1.18.30</lombok.version>
        <slf4j.version>2.0.9</slf4j.version>
        <logback.version>1.4.14</logback.version>
        <jackson.version>2.16.1</jackson.version>
        <owner.version>1.0.12</owner.version>

        <!-- Plugins Versions -->
        <maven-surefire-plugin.version>3.2.3</maven-surefire-plugin.version>
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <allure-maven.version>2.12.0</allure-maven.version>
        <aspectj.version>1.9.21</aspectj.version>

        <!-- Test Execution Parameters -->
        <thread.count>1</thread.count>
        <parallel.threads>4</parallel.threads>
        <max.parallel.browsers>3</max.parallel.browsers>
        <test.groups></test.groups>
        <browser>chrome</browser>
        <headless>false</headless>
    </properties>

    <dependencies>
        <!-- JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.version}</version>
        </dependency>

        <!-- Selenide -->
        <dependency>
            <groupId>com.codeborne</groupId>
            <artifactId>selenide</artifactId>
            <version>${selenide.version}</version>
        </dependency>

        <!-- Selenium WebDriver -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
        </dependency>

        <!-- WebDriverManager for automatic driver management -->
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>${webdrivermanager.version}</version>
        </dependency>

        <!-- AssertJ -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
        </dependency>

        <!-- Allure -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-junit5</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-selenide</artifactId>
            <version>${allure.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- Jackson for JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- Owner for configuration -->
        <dependency>
            <groupId>org.aeonbits.owner</groupId>
            <artifactId>owner</artifactId>
            <version>${owner.version}</version>
        </dependency>

        <!-- Cucumber Dependencies -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <version>1.10.1</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Maven Surefire Plugin for running tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <!-- Базовые настройки -->
                    <testFailureIgnore>false</testFailureIgnore>
                    <failIfNoTests>false</failIfNoTests>
                    <includes>
                        <include>**/CucumberRunner.java</include>
                    </includes>
                    
                    <!-- Настройки параллельности -->
                    <parallel>classes</parallel>
                    <threadCount>${parallel.threads}</threadCount>
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    
                    <groups>${test.groups}</groups>
                    
                    <systemPropertyVariables>
                        <!-- Allure -->
                        <allure.results.directory>${project.build.directory}/allure-results</allure.results.directory>
                        
                        <!-- Browser settings -->
                        <browser>${browser}</browser>
                        <headless>${headless}</headless>
                        <browser.headless>${headless}</browser.headless>
                        <max.parallel.browsers>${max.parallel.browsers}</max.parallel.browsers>
                        
                        <!-- JUnit Parallel Execution -->
                        <junit.jupiter.execution.parallel.enabled>true</junit.jupiter.execution.parallel.enabled>
                        <junit.jupiter.execution.parallel.mode.default>concurrent</junit.jupiter.execution.parallel.mode.default>
                        <junit.jupiter.execution.parallel.mode.classes.default>concurrent</junit.jupiter.execution.parallel.mode.classes.default>
                        <junit.jupiter.execution.parallel.config.strategy>fixed</junit.jupiter.execution.parallel.config.strategy>
                        <junit.jupiter.execution.parallel.config.fixed.parallelism>${parallel.threads}</junit.jupiter.execution.parallel.config.fixed.parallelism>
                        <junit.jupiter.execution.parallel.config.fixed.max-pool-size>${parallel.threads}</junit.jupiter.execution.parallel.config.fixed.max-pool-size>
                        
                        <!-- Cucumber Parallel Execution -->
                        <cucumber.execution.parallel.enabled>false</cucumber.execution.parallel.enabled>
                        <cucumber.execution.parallel.mode.default>concurrent</cucumber.execution.parallel.mode.default>
                        <cucumber.execution.parallel.mode.classes.default>concurrent</cucumber.execution.parallel.mode.classes.default>
                        <cucumber.execution.parallel.config.strategy>fixed</cucumber.execution.parallel.config.strategy>
                        <cucumber.execution.parallel.config.fixed.parallelism>${parallel.threads}</cucumber.execution.parallel.config.fixed.parallelism>
                    </systemPropertyVariables>
                    
                    <!-- JVM Arguments -->
                    <argLine>
                        -Xms2g -Xmx4g
                        -XX:+UseG1GC
                        -XX:+DisableExplicitGC
                        -XX:+UseStringDeduplication
                        -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"
                        -Dcucumber.plugin="io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm"
                        -Dallure.results.directory=${project.build.directory}/allure-results
                        -Dfile.encoding=UTF-8
                        -Dcucumber.publish.quiet=true
                    </argLine>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjweaver</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- Allure Maven Plugin -->
            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>${allure-maven.version}</version>
                <configuration>
                    <reportVersion>${allure.version}</reportVersion>
                    <resultsDirectory>${project.build.directory}/allure-results</resultsDirectory>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>

                    <!-- Generate XML reports for CI systems -->
                    <useFile>true</useFile>

                    <!-- Flaky test retry -->
                    <rerunFailingTestsCount>2</rerunFailingTestsCount>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Profile for running all tests -->
        <profile>
            <id>all-tests</id>
            <properties>
                <test.groups></test.groups>
            </properties>
        </profile>

        <!-- Profile for login tests -->
        <profile>
            <id>login</id>
            <properties>
                <test.groups>login</test.groups>
            </properties>
        </profile>

        <!-- Profile for inventory tests -->
        <profile>
            <id>inventory</id>
            <properties>
                <test.groups>inventory</test.groups>
            </properties>
        </profile>

        <!-- Profile for cart tests -->
        <profile>
            <id>cart</id>
            <properties>
                <test.groups>cart</test.groups>
            </properties>
        </profile>

        <!-- Profile for checkout tests -->
        <profile>
            <id>checkout</id>
            <properties>
                <test.groups>checkout</test.groups>
            </properties>
        </profile>

        <!-- Profile for navigation tests -->
        <profile>
            <id>navigation</id>
            <properties>
                <test.groups>navigation</test.groups>
            </properties>
        </profile>

        <!-- Profile for smoke tests -->
        <profile>
            <id>smoke</id>
            <properties>
                <test.groups>smoke</test.groups>
            </properties>
        </profile>

        <!-- Browser profiles -->
        <profile>
            <id>chrome</id>
            <properties>
                <browser>chrome</browser>
            </properties>
        </profile>

        <profile>
            <id>firefox</id>
            <properties>
                <browser>firefox</browser>
            </properties>
        </profile>

        <profile>
            <id>edge</id>
            <properties>
                <browser>edge</browser>
            </properties>
        </profile>

        <profile>
            <id>safari</id>
            <properties>
                <browser>safari</browser>
            </properties>
        </profile>

        <!-- Headless mode profile -->
        <profile>
            <id>headless</id>
            <properties>
                <headless>true</headless>
            </properties>
        </profile>

        <!-- Sequential execution profile (один тест за раз) -->
        <profile>
            <id>sequential</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven-surefire-plugin.version}</version>
                        <configuration>
                            <parallel>none</parallel>
                            <threadCount>1</threadCount>
                            <perCoreThreadCount>false</perCoreThreadCount>
                            <forkCount>1</forkCount>
                            <reuseForks>true</reuseForks>
                            <groups>${test.groups}</groups>
                            <systemPropertyVariables>
                                <allure.results.directory>${project.build.directory}/allure-results</allure.results.directory>
                                <junit.jupiter.execution.parallel.enabled>false</junit.jupiter.execution.parallel.enabled>
                                <selenide.browser>${browser}</selenide.browser>
                                <selenide.headless>${headless}</selenide.headless>
                            </systemPropertyVariables>
                            <argLine>
                                -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"
                            </argLine>
                            <properties>
                                <configurationParameters>
                                    junit.jupiter.execution.parallel.enabled = false
                                    junit.jupiter.execution.parallel.mode.default = same_thread
                                    junit.jupiter.execution.parallel.mode.classes.default = same_thread
                                </configurationParameters>
                            </properties>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Parallel execution profile for Cucumber -->
        <profile>
            <id>parallel</id>
            <properties>
                <parallel.threads>4</parallel.threads>
                <max.parallel.browsers>4</max.parallel.browsers>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven-surefire-plugin.version}</version>
                        <configuration>
                            <systemPropertyVariables>
                                <!-- Cucumber Parallel Execution -->
                                <cucumber.execution.parallel.enabled>true</cucumber.execution.parallel.enabled>
                                <cucumber.execution.parallel.config.strategy>fixed</cucumber.execution.parallel.config.strategy>
                                <cucumber.execution.parallel.config.fixed.parallelism>4</cucumber.execution.parallel.config.fixed.parallelism>
                                <cucumber.execution.parallel.config.fixed.max-pool-size>4</cucumber.execution.parallel.config.fixed.max-pool-size>
                                
                                <!-- JUnit Parallel Execution -->
                                <junit.jupiter.execution.parallel.enabled>true</junit.jupiter.execution.parallel.enabled>
                                <junit.jupiter.execution.parallel.config.strategy>fixed</junit.jupiter.execution.parallel.config.strategy>
                                <junit.jupiter.execution.parallel.config.fixed.parallelism>4</junit.jupiter.execution.parallel.config.fixed.parallelism>
                                <junit.jupiter.execution.parallel.config.fixed.max-pool-size>4</junit.jupiter.execution.parallel.config.fixed.max-pool-size>
                                
                                <!-- Browser settings -->
                                <browser>${browser}</browser>
                                <headless>${headless}</headless>
                                <browser.headless>${headless}</browser.headless>
                                <max.parallel.browsers>4</max.parallel.browsers>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        
        <!-- Parallel-strict profile with specific thread count -->
        <profile>
            <id>parallel-strict</id>
            <properties>
                <parallel.threads>${thread.count}</parallel.threads>
                <max.parallel.browsers>${thread.count}</max.parallel.browsers>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven-surefire-plugin.version}</version>
                        <configuration>
                            <systemPropertyVariables>
                                <!-- Cucumber Parallel Execution - переопределяем для динамического thread count -->
                                <cucumber.execution.parallel.enabled>true</cucumber.execution.parallel.enabled>
                                <cucumber.execution.parallel.config.strategy>fixed</cucumber.execution.parallel.config.strategy>
                                <cucumber.execution.parallel.config.fixed.parallelism>${thread.count}</cucumber.execution.parallel.config.fixed.parallelism>
                                <cucumber.execution.parallel.config.fixed.max-pool-size>${thread.count}</cucumber.execution.parallel.config.fixed.max-pool-size>
                                
                                <!-- JUnit Parallel Execution -->
                                <junit.jupiter.execution.parallel.enabled>true</junit.jupiter.execution.parallel.enabled>
                                <junit.jupiter.execution.parallel.config.strategy>fixed</junit.jupiter.execution.parallel.config.strategy>
                                <junit.jupiter.execution.parallel.config.fixed.parallelism>${thread.count}</junit.jupiter.execution.parallel.config.fixed.parallelism>
                                <junit.jupiter.execution.parallel.config.fixed.max-pool-size>${thread.count}</junit.jupiter.execution.parallel.config.fixed.max-pool-size>
                                
                                <!-- Browser settings -->
                                <browser>${browser}</browser>
                                <headless>${headless}</headless>
                                <browser.headless>${headless}</browser.headless>
                                <max.parallel.browsers>${thread.count}</max.parallel.browsers>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
